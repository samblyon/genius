const React = require('react');

const SongInfoStats = React.createClass({

  render() {
    return (
      <div class="song-info-stats">
        aggregate stats will go here!
      </div>
    );
  }

});

module.exports = SongInfoStats;
